# `tree-sitter generate` byproducts, regenerated from grammar.js. Only the
# grammar source (grammar.js, src/scanner.c) and the build inputs the C
# compiler needs (src/parser.c, src/tree_sitter/parser.h) are vendored.
/src/grammar.json
/src/node-types.json
/src/tree_sitter/alloc.h
/src/tree_sitter/array.h
