[package]
name = "stoat_host"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

# Compile the in-memory fakes. Off by default so a release build carries none
# of them, and enabled from a consumer's [dev-dependencies] so its tests do.
[features]
test-support = []

[dependencies]
compact_str.workspace = true
ignore.workspace = true
libc.workspace = true
notify.workspace = true
tempfile.workspace = true
